# Local environment
.env

# Per-machine rebuild config — Dropbox / iCloud paths shouldn't end up
# in the repo. Copy from banking-pipeline.example.toml and edit.
banking-pipeline.toml

# Personal UK-tax commodity metadata (held ISINs, reporting status).
# Copy from data/commodities.example.toml and edit.
data/commodities.toml

# Personal pre-ledger opening positions (historical cost basis).
# Copy from data/opening-positions.example.toml and edit.
data/opening-positions.toml

# Personal excess reportable income table (fund ERI + equalisation).
# Copy from data/eri.example.toml and edit.
data/eri.toml

# Personal pre-ledger CGT losses carried into the ledger era.
# Copy from data/cgt-losses.example.toml and edit.
data/cgt-losses.toml

# Personal residential property (off the investment ledger).
# Copy from data/property.example.toml and edit.
data/property.toml

# Generated / personal ledger data. The pipeline writes per-year ingest
# output, the portfolio/prices/balances aggregates, the transaction
# sidecars, and the *.md summaries here — all derived from personal
# statements. The committed *.example.toml and data/fx CSV are tracked
# and unaffected by these patterns.
data/*.beancount
data/*.transactions.jsonl
data/*.md
# Generated per-account ledgers (banking-pipeline portfolio-split).
# Named explicitly rather than data/*/ so the tracked data/fx rate CSV
# stays committed.
data/accounts/

# Hand-curated bean-check root — references the personal portfolio
# structure, so kept local rather than committed.
main.beancount

# Generated reports (personal financial output). Every per-report
# sub-directory is ignored (uk-tax, reconciliation, concentration,
# net-worth, income, allocation, portfolio-allocation, trial-balance, …)
# so a new report can't silently become committable; the hand-written
# build scripts / templates at the reports/ root stay visible.
reports/*/
reports/*.html
# …but keep the hand-written HTML template (source, not generated output).
!reports/*_template.html
reports/*.json

# Reports dashboard (prototype): the staged docs tree and the built site
# inline real figures — only mkdocs.yml / README.md / the generator are kept.
dashboard/docs/
dashboard/site/

# ECB daily FX history (~49k rows, regenerable via scripts/fetch_ecb_rates.py)
# — unlike the small, curated HMRC monthly CSV, this is a bulk auto-download.
data/fx/ecb-daily.csv

# macOS
.DS_Store

# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Track committed agent config (rules + subagents); ignore local-only bits.
.claude/*
!.claude/rules/
!.claude/agents/

# Local PII hard-block list for the pre-commit guard (scripts/check_pii.py)
.pii-deny
